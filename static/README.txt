이 폴더에 favicon.ico 등 정적 파일을 넣으세요.

· favicon.ico 파일을 이 폴더(static)에 복사한 뒤
· 관리자 → 시스템 → 환경 설정 에서
  "파비콘(ico) URL"에 다음 주소를 입력하세요:
  https://noti.icopay.net/static/favicon.ico

(테스트 서버라면 해당 도메인으로 바꿔서 입력)
