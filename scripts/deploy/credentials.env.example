# Local only — never commit. Copy to %USERPROFILE%\.noti-deploy\credentials.env
SSH_HOST=172.235.192.86
SSH_USER=root
SSH_PASSWORD=
SSH_PORT=22
REMOTE_NOTI_DIR=/srv/pg-noti
PM2_APP_NAME=pg-noti-relay
